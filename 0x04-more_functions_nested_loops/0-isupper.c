#include "main.h"
/**
 *_isupper - check if its an uppercase character
 *
 *return : return 1 for uppercase, 0 for lowercase
 */

int _isupper(int c)
{
if (c > 64 && c < 91)
{
return (1);
}
else
{
return (0);
}

}
